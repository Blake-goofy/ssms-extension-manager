<Window x:Class="SsmsExtensionManager.App.SourceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Update Source"
        Height="210"
        Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">
    <Border Background="{DynamicResource AppBackgroundBrush}"
            Padding="18"
            TextElement.Foreground="{DynamicResource ControlForegroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="GitHub release source" FontSize="16" FontWeight="SemiBold" />
            <TextBlock Grid.Row="1"
                       Text="Enter owner/repo or a GitHub repository URL."
                       Foreground="{DynamicResource MutedForegroundBrush}"
                       Margin="0,8,0,6" />
            <Grid Grid.Row="2"
                  VerticalAlignment="Top"
                  Height="30">
                <TextBox x:Name="SourceUriTextBox"
                         VerticalContentAlignment="Center"
                         Padding="6,0"
                         TextChanged="SourceUriTextBox_TextChanged" />
                <TextBlock x:Name="SourcePlaceholderText"
                           Text="owner/repo"
                           Margin="8,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource MutedForegroundBrush}"
                           IsHitTestVisible="False"
                           Visibility="Collapsed" />
            </Grid>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Padding="12,6" Margin="0,0,8,0" Click="Cancel_Click" IsCancel="True" />
                <Button Content="Save" Padding="12,6" Click="Save_Click" IsDefault="True" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
