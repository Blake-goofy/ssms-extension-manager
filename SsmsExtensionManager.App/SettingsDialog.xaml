<Window x:Class="SsmsExtensionManager.App.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="230"
        Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        PreviewMouseLeftButtonDown="Window_PreviewMouseLeftButtonDown">
    <Border Background="{DynamicResource AppBackgroundBrush}"
            Padding="18"
            TextElement.Foreground="{DynamicResource ControlForegroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0"
                   Grid.Column="0"
                   Content="_SSMS instance"
                   Target="{Binding ElementName=InstanceDropDownButton}"
                   Padding="0,4,10,4" />
            <Grid Grid.Row="0"
                  Grid.Column="1"
                  MinWidth="360">
                <Button x:Name="InstanceDropDownButton"
                        Padding="8,5"
                        HorizontalContentAlignment="Stretch"
                        Click="InstanceDropDown_Click">
                    <DockPanel LastChildFill="True">
                        <Path DockPanel.Dock="Right"
                              Width="10"
                              Height="6"
                              Margin="10,0,0,0"
                              VerticalAlignment="Center"
                              Stroke="{DynamicResource MutedForegroundBrush}"
                              StrokeThickness="1.5"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              Data="M 0 1.5 L 5 4.5 L 10 1.5" />
                        <TextBlock x:Name="InstanceDropDownText"
                                   TextTrimming="CharacterEllipsis" />
                    </DockPanel>
                </Button>
                <Popup x:Name="InstanceDropDownPopup"
                       AllowsTransparency="True"
                       Placement="Bottom"
                       PlacementTarget="{Binding ElementName=InstanceDropDownButton}"
                       PopupAnimation="Fade"
                       StaysOpen="True">
                    <Border Background="{DynamicResource PanelBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            MinWidth="{Binding ActualWidth, ElementName=InstanceDropDownButton}"
                            MaxHeight="220"
                            Padding="3">
                        <ListBox x:Name="InstanceListBox"
                                 Background="{DynamicResource PanelBackgroundBrush}"
                                 BorderThickness="0"
                                 DisplayMemberPath="Display"
                                 Foreground="{DynamicResource ControlForegroundBrush}"
                                 SelectionChanged="InstanceListBox_SelectionChanged">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="{x:Type ListBoxItem}">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="Foreground" Value="{DynamicResource ControlForegroundBrush}" />
                                    <Setter Property="Padding" Value="8,5" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                <Border x:Name="Chrome"
                                                        Background="{TemplateBinding Background}"
                                                        CornerRadius="3"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource MenuItemHoverBrush}" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SelectionBackgroundBrush}" />
                                                        <Setter Property="Foreground" Value="{DynamicResource SelectionForegroundBrush}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Border>
                </Popup>
            </Grid>

            <CheckBox Grid.Row="1"
                      Grid.Column="1"
                      x:Name="ShowMicrosoftExtensionsCheckBox"
                      Content="_Show extensions published by Microsoft"
                      Margin="0,12,0,0" />

            <CheckBox Grid.Row="2"
                      Grid.Column="1"
                      x:Name="DarkThemeCheckBox"
                      Content="_Dark theme"
                      Margin="0,12,0,0"
                      VerticalAlignment="Top" />

            <StackPanel Grid.Row="3"
                        Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="_Cancel" Padding="12,6" Margin="0,0,8,0" Click="Cancel_Click" />
                <Button Content="_Save" Padding="12,6" Click="Save_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
